# Synthetic local-only values. Never use these in production.
POSTGRES_DB=queue
POSTGRES_USER=queue
POSTGRES_PASSWORD=local-only-postgres-password
QUEUE_HMAC_KEY=0123456789abcdef0123456789abcdef
QUEUE_HMAC_KEY_VERSION=v1
QUEUE_ADMIN_TOKEN=local-only-admin-token-0123456789abcdef
DOMAIN_TOKEN=
WEB_PORT=8080
